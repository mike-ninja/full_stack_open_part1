import React from 'react'
import ReactDOM from 'react-dom/client'

import App from './App'

// This renders the component named app into a div element
ReactDOM.createRoot(document.getElementById('root')).render(<App />)
